buildscript {
    // Define versions in a single place
    extra.apply {
        set(
            "appName",
            "HomeWeatherClock"
        )
        set(
            "appPackageName",
            "ru.homeweatherclock.adg"
        )
        set(
            "appVersionName",
            "1.0.1"
        )
        set(
            "appVersionCode",
            1
        )
    }
}